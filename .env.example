# MySQL root 密码
MYSQL_ROOT_PASSWORD=your_production_password_here

BASE_INFO_API_URL=http://backend-base:8081

# 构建方式: local（本地构建）| registry（拉取远端镜像）
BUILD_MODE=registry

# 远端镜像仓库地址（BUILD_MODE=registry 时生效）
REGISTRY=ghcr.io/uppi7
